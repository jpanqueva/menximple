# Núcleo (siempre)
# Fijada: `>=` dejo que un rebuild del 17-sep cambiara 3.4 -> 4.0.4 sin avisar.
# Subirla es un cambio deliberado, con las pruebas corridas en la version nueva.
fastmcp==4.0.4
qdrant-client>=1.9
pydantic>=2.7
pydantic-settings>=2.3

# Opcional (embeddings del resumen) — instalar solo si EMBEDDINGS_ENABLED=true:
#   pip install -r requirements-embeddings.txt
